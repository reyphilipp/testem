package konto

/*class Depositkonto(ktoNummer:String): Konto(ktoNummer) {
    private var ktoStand = 0.0
    override fun zahleEin(betrag:Double):String {
        if (betrag < 0) {
            return "nicht einbezahlt"
        }
        ktoStand = ktoStand + betrag
        println("Auf Sparkonto eibezahlt: $ktoStand")
        return "einbezahlt"
    }
    override fun getKontostand(){
        println("Depositkontostand ist:$ktoStand")
    }
}*/